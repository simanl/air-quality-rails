import { formattedTemperature } from 'frontend/helpers/formatted-temperature';
import { module, test } from 'qunit';

module('Unit | Helper | formattedTemperature');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formattedTemperature([42]);
  assert.ok(result);
});
