import { mapMarkerIconByQuality } from 'frontend/helpers/map-marker-icon-by-quality';
import { module, test } from 'qunit';

module('Unit | Helper | map marker icon by quality');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mapMarkerIconByQuality([42]);
  assert.ok(result);
});
